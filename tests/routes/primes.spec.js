//chai
var chai = require('chai');
var assert = chai.assert;

describe ('routes/Primes', () => {
    describe('route', () => {
        //success cases
        it('should return multiplication table of primes up to param n', () => assert.isTrue(false));

        //fail cases
        it('should return an invalid input response if n is invalid', () => assert.isTrue(false));
    });
});