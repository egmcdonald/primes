//chai
var chai = require('chai');
var assert = chai.assert;

describe('server', () => {
    describe('/', () => {
        it('should return ok and useful response as to how to use application', () =>  assert.isTrue(false));
    });

    describe('/:n', () => {
        it('should return ok and prime multiplication table', () =>  assert.isTrue(false));
    });
});